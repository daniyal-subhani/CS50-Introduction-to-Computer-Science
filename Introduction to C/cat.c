#include <stdio.h>

int main() {
    int number;
    printf("Type a number: ");
    scanf("%d", &number);
    for (int i = 0; i <= number; i++)
    {
        /* code */
        printf("meow \n");
    }
    
    return 0;
}