#include <stdio.h>

int main() {
    int condition = 0;
    while (condition <= 5)
    {
        /* code */
        printf("Condition is: %d\n", condition);
        condition++;
    }
    
    return 0;
}